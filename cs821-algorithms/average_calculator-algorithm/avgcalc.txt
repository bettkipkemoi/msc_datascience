declare scores: array(n) of integers
initialize sum: integer
initialize average: real

scores <- []

sum <- calculate total
average <- 0.0
for i in range(1:len(scores)):
    sum = sum +scores(i)

    average = sum/len(scores)

print(average)