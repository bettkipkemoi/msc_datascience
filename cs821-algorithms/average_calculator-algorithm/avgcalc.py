declare scores: array(n) of integers
declare total: integer
declare average: real

scores <- []

total <- calculate total
average <- 0.0
for i in range(1:len(scores)):
    total = total +scores(i)

    average = total/len(scores)

print(average)